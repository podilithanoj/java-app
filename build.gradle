plugins {
    id 'java'
    id 'maven-publish'
}

group = 'com.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
            groupId = 'com.example'
            artifactId = 'gradle-java-app'
            version = '1.0-SNAPSHOT'
        }
    }

    repositories {
        maven {
            name = "nexus"
            url = uri("http://54.81.136.248:8081/repository/maven-snapshots/")
            allowInsecureProtocol = true
            credentials {
                username = "admin"
                password = "Admin@123"
            }
        }
    }
}
