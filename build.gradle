plugins {
    id 'java'
    id 'maven-publish'
}

group = 'com.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    // your dependencies
}

jar {
    manifest {
        attributes(
            'Main-Class': 'com.example.Main' // ✅ UPDATE with your actual entry point class
        )
    }
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
    repositories {
        maven {
            name = "Nexus"
            url = uri("http://54.81.136.248:8081/repository/maven-snapshots")
            credentials {
                username = "admin"
                password = "Admin@123"
            }
        }
    }
}
